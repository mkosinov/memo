/**
 * Domain logic for bookings.
 *
 * Currently empty — business rules (validation, pricing, eligibility)
 * will live here once defined.
 *
 * @see model/view/booking.ts  — UI form state (PrivateBookingView)
 * @see lib/transforms/to-booking-dto.ts  — PrivateBookingView → DTO mapper
 */
export {};
