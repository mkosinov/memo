// Standard modal height for all admin modals. Fixed (not max-height)
// to prevent "jump" when switching tabs inside a modal.
export const MODAL_CONTAINER_CLASS = 'flex flex-col overflow-hidden h-[85vh]';
export const MODAL_MAX_WIDTH = 'max-w-2xl';

// Small variant (compact forms: single field, no scrollable lists)
export const MODAL_SMALL_CLASS = 'flex flex-col overflow-hidden h-[60vh]';
export const MODAL_SMALL_MAX_WIDTH = 'max-w-lg';
