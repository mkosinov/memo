'use client';

/**
 * Placeholder metrics block — will be populated when the metrics pattern
 * from /clients is ported here (visit count, total spent, last visit, etc.).
 */
export function RecordMetrics() {
  return (
    <div
      className="rounded-lg border p-3 text-xs text-ink-light"
      style={{ borderColor: 'var(--line)' }}
      data-testid="record-metrics"
    >
      {/* TODO: metrics from /clients — visit count, total spent, last visit */}
    </div>
  );
}
