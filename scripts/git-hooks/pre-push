#!/usr/bin/env bash
# Pre-push hook: DISABLED.
# Tests run in GitHub Actions CI (test.yml + smoke.yml) on push and PR.
# This hook is a no-op (exit 0).
#
# To run the full local test suite manually:
#   VISUAL_COMPLIANCE=0 bash scripts/test-all.sh
#
# To re-enable the hook, replace this file with the original test runner:
#   exec "$REPO_ROOT/scripts/test-all.sh"

exit 0